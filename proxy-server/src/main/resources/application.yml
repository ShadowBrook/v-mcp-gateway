server:
  port: 8080

mcp:
  servers:
    # SSE MCP server
    - name: sse-demo
      type: sse
      url: http://localhost:7080/sse
    # Streamable HTTP MCP server
    - name: sse-demo-flux
      type: streamable-http
      url: http://localhost:7081/mcp
    # Stdio MCP server
    # - name: stdio-demo
    #   type: stdio
    #   command: node
    #   args:
    #     - /path/to/mcp-server.js
    #   env:
    #     KEY: value

  # HTTP tools — executed locally by ToolExecutor (not proxied to backend MCP)
  tools:
    - name: getCalendarInfo
      description: 获取万年历信息
      method: GET
      url: https://cn.apihz.cn/api/time/getday.php?id=88888888&key=88888888
